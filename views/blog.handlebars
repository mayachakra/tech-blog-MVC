<!-- The comment-details partial is a reusable component that displays the details of a single comment, 
including the comment text, the username of the user who made the comment, and the date the comment was created.
Here's how you can use these partials in your Handlebars templates:

In the blog.handlebars template, you can include the blog-details partial and pass the post object as a context variable.
 -->
{{> blog-details post=post}}
